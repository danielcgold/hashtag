var app = require('../index');
var http = require('http');

app.set('port', (process.env.PORT || 3000))

app.listen(app.get('port'), function() {
  console.log("Start making hashtags at http://localhost:" + app.get('port'))
})
